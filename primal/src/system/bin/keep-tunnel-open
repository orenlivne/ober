#!/bin/bash
#----------------------------------------------------------------
# Heartbeat to keep an SSH tunnel open and prevent timeout.
#----------------------------------------------------------------

while true; do sleep 10; ls; done
